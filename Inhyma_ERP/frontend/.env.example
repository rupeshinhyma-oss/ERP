# Absolute backend origin. Leave blank to use same-origin relative paths
# (recommended in dev: vite.config.ts proxies /api to the backend).
VITE_API_ORIGIN=
# Target the dev-server proxy forwards /api to.
VITE_API_PROXY_TARGET=http://localhost:8002
VITE_PORT=5174
VITE_APP_TITLE=Inhyma ERP
