# =============================================================================
# ERP_Main Backend - Global Control Plane
# Python 3.11+
#
# Deliberately mirrors Yinglima_ERP's dependency choices (same framework,
# ORM, and migration tool) so the two codebases share one set of
# conventions -- not because ERP_Main needs everything Yinglima needs.
# Phase 2 was infrastructure-only (no auth). Phase 3 adds platform-admin
# authentication and ERP-service credential authentication -- see
# argon2-cffi/pyjwt below, added for exactly that and nothing else.
# =============================================================================

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Data validation / settings ---
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Database ---
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0          # async PostgreSQL driver (runtime)
psycopg2-binary==2.9.10  # sync driver (used by Alembic migrations)
greenlet==3.1.1          # required by SQLAlchemy async engine (greenlet_spawn)
aiosqlite==0.20.0        # async SQLite driver (local dev / tests, mirrors Yinglima)

# --- Authentication / Security (Phase 3) ---
argon2-cffi==23.1.0      # Argon2id hashing: platform-admin passwords + service-credential secrets
pyjwt==2.10.1            # JWT for platform-admin session tokens (control-plane only, not ERP-local JWTs)
cryptography==43.0.3     # RSA keys & certificates for federation tokens (app.federation)
email-validator==2.2.0   # Pydantic EmailStr validation

# --- Utilities ---
python-dotenv==1.0.1
orjson==3.10.14
openpyxl==3.1.5          # Reporting Excel export generation

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.2
httpx==0.28.1
